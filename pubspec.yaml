name: taller_segundo_plano
description: "Proyecto Flutter para demostrar asincronía, Timer e Isolates."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # ⏱️ Para manejar timers y actualizaciones reactivas
  provider: ^6.1.2

  # 🧠 Para manejar operaciones asincrónicas con facilidad
  async: ^2.11.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
